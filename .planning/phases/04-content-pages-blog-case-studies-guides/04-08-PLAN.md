---
phase: 04-content-pages-blog-case-studies-guides
plan: 08
type: execute
wave: 8
depends_on: [04-01, 04-02, 04-03]
files_modified:
  - frontend/src/app/blog/[slug]/page.tsx
  - frontend/src/app/case-studies/[slug]/page.tsx
  - frontend/src/components/page-clients/case-study-client.tsx
autonomous: true
gap_closure: true
requirements: [BLOG-10, CS-01, CS-02]

must_haves:
  truths:
    - "Visiting /blog/ai-automation-eliminates-manual-work produces a rendered HTML page, not a 404"
    - "Visiting /blog/seo-age-of-ai-llm-answers produces a rendered HTML page, not a 404"
    - "Visiting /case-studies/finflows-back-office-automation produces a rendered HTML page, not a 404"
    - "Visiting /case-studies/growthloop-linkedin-scale produces a rendered HTML page, not a 404"
    - "case-study-client.tsx uses API_BASE_URL from @/lib/constants for all case_studies.php fetch calls"
  artifacts:
    - path: "frontend/src/app/blog/[slug]/page.tsx"
      provides: "generateStaticParams with seeded blog slugs"
      contains: "ai-automation-eliminates-manual-work"
    - path: "frontend/src/app/case-studies/[slug]/page.tsx"
      provides: "generateStaticParams with seeded case study slugs"
      contains: "finflows-back-office-automation"
    - path: "frontend/src/components/page-clients/case-study-client.tsx"
      provides: "Case study detail fetch using API_BASE_URL"
      contains: "API_BASE_URL"
  key_links:
    - from: "frontend/src/app/blog/[slug]/page.tsx"
      to: "frontend/out/blog/ai-automation-eliminates-manual-work/index.html"
      via: "generateStaticParams returning {slug: 'ai-automation-eliminates-manual-work'}"
      pattern: "ai-automation-eliminates-manual-work"
    - from: "frontend/src/components/page-clients/case-study-client.tsx"
      to: "backend/api/case_studies.php"
      via: "fetch using API_BASE_URL from @/lib/constants"
      pattern: "\\$\\{API_BASE_URL\\}/case_studies\\.php"
---

<objective>
Fix three static export gaps identified in the Phase 4 verification report. Two blockers: `generateStaticParams` in the blog and case study detail routes contain old demo slugs that don't match the seeded content slugs, causing 404s on the static-exported site. One warning: `case-study-client.tsx` uses hardcoded `/backend/api/case_studies.php` instead of `API_BASE_URL`, inconsistent with every other component.

Purpose: Seeded blog posts and case studies must be reachable via their detail page URLs after `next build`. The API_BASE_URL fix ensures the case study detail page works in all environments, not just production.
Output: Three files updated. `next build` produces HTML for all four seeded content slugs. `case-study-client.tsx` uses `API_BASE_URL` for both fetch calls.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-content-pages-blog-case-studies-guides/04-CONTEXT.md
@.planning/phases/04-content-pages-blog-case-studies-guides/04-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix generateStaticParams slugs in blog and case study detail routes</name>
  <files>
    frontend/src/app/blog/[slug]/page.tsx
    frontend/src/app/case-studies/[slug]/page.tsx
  </files>

  <read_first>
    - frontend/src/app/blog/[slug]/page.tsx — the full file; confirm which slugs are currently listed in generateStaticParams
    - frontend/src/app/case-studies/[slug]/page.tsx — the full file; confirm which slugs are currently listed in generateStaticParams
  </read_first>

  <action>
**blog/[slug]/page.tsx:**

Replace the entire `generateStaticParams` function body with the four seeded slug entries. The file is 21 lines; only the return array changes. Keep all other code (BlogDetailsClientWrapper, Page component) exactly as-is.

Current (wrong) slugs:
```
{ slug: "ai-automation-eliminating-manual-work" },
{ slug: "lead-qualification-bot" },
{ slug: "automation-platform-comparison" },
{ slug: "roi-framework-ai-automation" },
```

Replace with (correct seeded slugs, per init_db.php INSERT IGNORE rows):
```typescript
export async function generateStaticParams() {
  return [
    { slug: "ai-automation-eliminates-manual-work" },
    { slug: "seo-age-of-ai-llm-answers" },
  ];
}
```

**case-studies/[slug]/page.tsx:**

Replace the entire `generateStaticParams` function body with the two seeded slug entries. The file is 15 lines; only the return array changes. Keep Page component exactly as-is.

Current (wrong) slugs:
```
{ slug: "finflows-automation" },
{ slug: "retailbot-ai-support" },
{ slug: "proptrack-lead-automation" },
{ slug: "medsync-clinic-automation" },
```

Replace with (correct seeded slugs, per init_db.php INSERT IGNORE rows):
```typescript
export async function generateStaticParams() {
  return [
    { slug: "finflows-back-office-automation" },
    { slug: "growthloop-linkedin-scale" },
  ];
}
```

Do NOT alter any other line in either file. These are the exact slugs seeded in `backend/init_db.php` via INSERT IGNORE.
  </action>

  <verify>
    <automated>
      node -e "
        const fs = require('fs');
        const blog = fs.readFileSync('frontend/src/app/blog/[slug]/page.tsx','utf8');
        const cs = fs.readFileSync('frontend/src/app/case-studies/[slug]/page.tsx','utf8');
        const checks = [
          blog.includes('ai-automation-eliminates-manual-work'),
          blog.includes('seo-age-of-ai-llm-answers'),
          !blog.includes('ai-automation-eliminating-manual-work'),
          !blog.includes('lead-qualification-bot'),
          cs.includes('finflows-back-office-automation'),
          cs.includes('growthloop-linkedin-scale'),
          !cs.includes('finflows-automation'),
          !cs.includes('retailbot-ai-support'),
        ];
        const pass = checks.every(Boolean);
        console.log(pass ? 'PASS: All slug checks passed' : 'FAIL: ' + checks.map((c,i)=>'['+i+']='+c).join(', '));
        process.exit(pass ? 0 : 1);
      "
    </automated>
  </verify>

  <acceptance_criteria>
    - `frontend/src/app/blog/[slug]/page.tsx` contains the string `ai-automation-eliminates-manual-work` (note: "eliminates", not "eliminating")
    - `frontend/src/app/blog/[slug]/page.tsx` contains the string `seo-age-of-ai-llm-answers`
    - `frontend/src/app/blog/[slug]/page.tsx` does NOT contain `lead-qualification-bot`
    - `frontend/src/app/blog/[slug]/page.tsx` does NOT contain `ai-automation-eliminating-manual-work`
    - `frontend/src/app/case-studies/[slug]/page.tsx` contains the string `finflows-back-office-automation`
    - `frontend/src/app/case-studies/[slug]/page.tsx` contains the string `growthloop-linkedin-scale`
    - `frontend/src/app/case-studies/[slug]/page.tsx` does NOT contain `finflows-automation` (old slug)
    - `frontend/src/app/case-studies/[slug]/page.tsx` does NOT contain `retailbot-ai-support`
  </acceptance_criteria>

  <done>Both generateStaticParams functions return only the seeded content slugs. `next build` will generate HTML for all four seeded URLs.</done>
</task>

<task type="auto">
  <name>Task 2: Replace hardcoded /backend/api path with API_BASE_URL in case-study-client.tsx</name>
  <files>
    frontend/src/components/page-clients/case-study-client.tsx
  </files>

  <read_first>
    - frontend/src/components/page-clients/case-study-client.tsx — the full file; locate both `fetch('/backend/api/case_studies.php')` occurrences (lines 171 and 177 per verification) and the existing import block at the top
  </read_first>

  <action>
Two changes to `case-study-client.tsx`:

**1. Add import for API_BASE_URL** (at the top of the file, after existing imports):

The file currently imports from `@/components/blocks/floating-icons-hero-demo`, `@/components/ui/footer-section`, `@/components/ui/badge`, `@/components/ui/button`, `@/components/blocks/connect-cta`, and `lucide-react`. Add this import alongside the others (insert after the last `@/` import line):

```typescript
import { API_BASE_URL } from "@/lib/constants";
```

**2. Replace both hardcoded fetch paths:**

Line 171 — replace:
```typescript
fetch(`/backend/api/case_studies.php?slug=${slug}`)
```
with:
```typescript
fetch(`${API_BASE_URL}/case_studies.php?slug=${slug}`)
```

Line 177 — replace:
```typescript
fetch("/backend/api/case_studies.php")
```
with:
```typescript
fetch(`${API_BASE_URL}/case_studies.php`)
```

No other changes. Do not modify component logic, types, rendering, or any other fetch call.

**Why:** `API_BASE_URL` resolves to `https://digi.cluxn.com/backend/api` in production (via `NEXT_PUBLIC_API_URL` env var, falling back to `https://digi.cluxn.com/backend/api` in `@/lib/constants`). Using the relative `/backend/api` path works only when frontend and backend share the same origin — this is an inconsistency with all other components in the project (blog-details-client.tsx, guide-client.tsx, etc.) that already use `API_BASE_URL`.
  </action>

  <verify>
    <automated>
      node -e "
        const fs = require('fs');
        const src = fs.readFileSync('frontend/src/components/page-clients/case-study-client.tsx','utf8');
        const checks = [
          src.includes('API_BASE_URL'),
          src.includes('from \"@/lib/constants\"') || src.includes(\"from '@/lib/constants'\"),
          src.includes('\${API_BASE_URL}/case_studies.php?slug='),
          src.includes('\${API_BASE_URL}/case_studies.php\`'),
          !src.includes('/backend/api/case_studies.php'),
        ];
        const pass = checks.every(Boolean);
        console.log(pass ? 'PASS: API_BASE_URL pattern verified' : 'FAIL: ' + checks.map((c,i)=>'['+i+']='+c).join(', '));
        process.exit(pass ? 0 : 1);
      "
    </automated>
  </verify>

  <acceptance_criteria>
    - `case-study-client.tsx` imports `API_BASE_URL` from `@/lib/constants`
    - The string `/backend/api/case_studies.php` does NOT appear anywhere in the file (grep returns 0 matches)
    - The pattern `${API_BASE_URL}/case_studies.php?slug=` appears for the single-item fetch
    - The pattern `${API_BASE_URL}/case_studies.php` (backtick string) appears for the list fetch
    - TypeScript compiles clean: running `npx tsc --noEmit` from `frontend/` returns exit code 0
  </acceptance_criteria>

  <done>case-study-client.tsx uses API_BASE_URL for all case_studies.php fetch calls. No hardcoded /backend/api paths remain in the file.</done>
</task>

<task type="auto">
  <name>Task 3: TypeScript compile check across all three modified files</name>
  <files>
    (no files modified — verification only)
  </files>

  <read_first>
    - frontend/src/app/blog/[slug]/page.tsx — confirm final state after Task 1
    - frontend/src/app/case-studies/[slug]/page.tsx — confirm final state after Task 1
    - frontend/src/components/page-clients/case-study-client.tsx — confirm final state after Task 2
  </read_first>

  <action>
Run TypeScript type-check from the frontend directory to confirm no type errors were introduced by the changes in Tasks 1 and 2.

```bash
cd frontend && npx tsc --noEmit
```

If the output is empty (exit code 0), all three files compile cleanly. If there are errors, read the error messages and fix them — the most likely cause would be a malformed template literal in Task 2's fetch replacement or a missing import.

Do NOT run `next build` — tsc --noEmit is sufficient to verify types without triggering a full static export.
  </action>

  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1; echo "Exit: $?"</automated>
  </verify>

  <acceptance_criteria>
    - `npx tsc --noEmit` completes with exit code 0
    - No TypeScript errors printed to stdout/stderr
    - All three files (blog/[slug]/page.tsx, case-studies/[slug]/page.tsx, case-study-client.tsx) are syntactically valid TypeScript
  </acceptance_criteria>

  <done>TypeScript reports zero errors across all modified files. The gap closure changes are type-safe.</done>
</task>

</tasks>

<verification>
After all tasks complete, verify the three gaps from VERIFICATION.md are closed:

**Gap 1 closed:** `grep "ai-automation-eliminates-manual-work" frontend/src/app/blog/\[slug\]/page.tsx` returns a match. `grep "lead-qualification-bot" frontend/src/app/blog/\[slug\]/page.tsx` returns no match.

**Gap 2 closed:** `grep "finflows-back-office-automation" frontend/src/app/case-studies/\[slug\]/page.tsx` returns a match. `grep "finflows-automation" frontend/src/app/case-studies/\[slug\]/page.tsx` returns no match.

**Gap 3 closed:** `grep "/backend/api/case_studies.php" frontend/src/components/page-clients/case-study-client.tsx` returns no matches. `grep "API_BASE_URL" frontend/src/components/page-clients/case-study-client.tsx` returns 3+ matches (import + 2 fetch calls).

**TypeScript clean:** `cd frontend && npx tsc --noEmit` exits 0.
</verification>

<success_criteria>
- Both blog slugs (`ai-automation-eliminates-manual-work`, `seo-age-of-ai-llm-answers`) are in `generateStaticParams` in blog/[slug]/page.tsx
- Both case study slugs (`finflows-back-office-automation`, `growthloop-linkedin-scale`) are in `generateStaticParams` in case-studies/[slug]/page.tsx
- No old demo slugs remain in either generateStaticParams function
- `case-study-client.tsx` imports `API_BASE_URL` from `@/lib/constants` and uses it for both fetch calls
- The string `/backend/api/case_studies.php` does not appear anywhere in case-study-client.tsx
- `npx tsc --noEmit` exits 0
- All 20 verification truths from 04-VERIFICATION.md are now satisfied (was 17/20)
</success_criteria>

<output>
After completion, create `.planning/phases/04-content-pages-blog-case-studies-guides/04-08-SUMMARY.md`
</output>
