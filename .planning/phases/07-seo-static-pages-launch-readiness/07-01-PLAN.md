---
phase: 07-seo-static-pages-launch-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/thank-you/page.tsx
  - frontend/src/app/not-found.tsx
  - frontend/src/app/error.tsx
  - frontend/src/app/privacy-policy/page.tsx
  - frontend/src/app/terms-and-conditions/page.tsx
autonomous: true
requirements: [STATIC-01, STATIC-02, STATIC-03, STATIC-04, STATIC-05]

must_haves:
  truths:
    - "Thank you page shows 'Transmission Success' with secondary nav links for Services and Blog"
    - "Visiting an unknown URL shows a branded 404 page with '404 — Signal Lost' badge"
    - "The error page has a 'Try Again' retry button and a 'Return Home' secondary link"
    - "Privacy Policy page contains sections on AI data processing, GDPR, Calendly, Google Analytics"
    - "Terms & Conditions page contains sections on IP ownership, SLAs, liability limits, data ownership"
  artifacts:
    - path: "frontend/src/app/not-found.tsx"
      provides: "Custom 404 page — new file"
    - path: "frontend/src/app/thank-you/page.tsx"
      provides: "Thank you page with secondary nav links"
    - path: "frontend/src/app/error.tsx"
      provides: "GlobalError page with Try Again + Return Home"
    - path: "frontend/src/app/privacy-policy/page.tsx"
      provides: "Expanded legal privacy policy content"
    - path: "frontend/src/app/terms-and-conditions/page.tsx"
      provides: "Expanded legal T&C content"
  key_links:
    - from: "app/not-found.tsx"
      to: "app/layout.tsx"
      via: "inherits root layout — NO manual Navbar/Footer import"
      pattern: "no Navbar|Footer import in not-found.tsx"
    - from: "app/error.tsx"
      to: "reset prop"
      via: "GlobalError interface — must keep reset function prop"
      pattern: "reset\\(\\)"
---

<objective>
Redesign all static and legal pages to match the site design system and add the missing custom 404 page.

Purpose: Phase 7 launch readiness — every user-facing static page must look professional and on-brand before going live. Legal pages need proper content for a digital marketing automation agency.
Output: 5 files (4 redesigned, 1 new) — all static/legal pages complete and design-system-consistent.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-seo-static-pages-launch-readiness/07-CONTEXT.md
@.planning/phases/07-seo-static-pages-launch-readiness/07-UI-SPEC.md

@frontend/src/app/globals.css
@frontend/src/app/thank-you/page.tsx
@frontend/src/app/error.tsx
@frontend/src/app/privacy-policy/page.tsx
@frontend/src/app/terms-and-conditions/page.tsx
@frontend/src/app/layout.tsx
</context>

<interfaces>
<!-- Design system classes confirmed in globals.css and existing static pages -->

CSS utility classes (from globals.css):
- `.hero-title` — large display heading, weight 900, 80–84px fluid
- `.hero-title-accent` — brand gradient on span text (teal to violet)
- `.section-eyebrow` — small badge text style (brand violet, uppercase, tracking)
- `.section-subtitle` — subheading prose, 16px, leading-relaxed

Component imports:
```typescript
import { Badge } from "@/components/ui/badge"
import { Button } from "@/components/ui/button"
import Link from "next/link"
// For thank-you and error pages only (they currently import Navbar/Footer manually):
import { Navbar } from "@/components/blocks/floating-icons-hero-demo"
import { Footer } from "@/components/ui/footer-section"
// NOTE: not-found.tsx MUST NOT import Navbar or Footer — it inherits from root layout
```

Button variants:
- `variant="brand"` — primary CTA (violet gradient)
- `variant="outline"` — secondary link (outlined, no fill)

Color tokens (from globals.css @theme):
- `--color-brand: #7C3AED` (violet)
- `--color-base: #F8FAFC` (page background)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Redesign Thank You page + create not-found.tsx + update error.tsx</name>
  <files>frontend/src/app/thank-you/page.tsx, frontend/src/app/not-found.tsx, frontend/src/app/error.tsx</files>

  <read_first>
    - frontend/src/app/thank-you/page.tsx (current implementation — add secondary nav links only)
    - frontend/src/app/error.tsx (current implementation — update button copy, add Return Home link)
    - frontend/src/app/layout.tsx (confirm root layout structure — not-found.tsx inherits this)
    - frontend/src/app/globals.css (confirm .hero-title, .hero-title-accent, .section-eyebrow, .section-subtitle class names)
  </read_first>

  <action>
**thank-you/page.tsx** — add secondary navigation link row below primary CTA button. Keep all existing code; only add the secondary buttons below the existing `Button asChild variant="brand"` element:
```tsx
// Add after the existing brand Button (inside the flex div):
<Button asChild variant="outline" className="px-8 h-14 font-bold">
  <Link href="/services">Explore Services</Link>
</Button>
<Button asChild variant="outline" className="px-8 h-14 font-bold">
  <Link href="/blog">Read Our Blog</Link>
</Button>
```
Keep all other code unchanged — "Transmission Success" heading, badge, subtitle, radial gradient background, Navbar, Footer imports.

**not-found.tsx** — create new file at `frontend/src/app/not-found.tsx`. This is a Server Component (NO "use client"). Do NOT import Navbar or Footer — they are inherited from root layout.tsx automatically.
```tsx
// frontend/src/app/not-found.tsx
import Link from "next/link";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";

export default function NotFound() {
  return (
    <section className="relative min-h-screen flex items-center justify-center py-32 overflow-hidden bg-base">
      <div className="absolute inset-0 bg-[radial-gradient(ellipse_at_center,rgba(124,58,237,0.06),transparent_70%)]" />
      <div className="relative z-10 max-w-2xl mx-auto px-6 text-center">
        <Badge variant="outline" className="section-eyebrow mb-6 mx-auto">
          404 — Signal Lost
        </Badge>
        <h1 className="hero-title mb-6 leading-[1.05]">
          Lost in the{" "}
          <span className="hero-title-accent">automation void</span>?
        </h1>
        <p className="section-subtitle opacity-70 mb-10">
          This page doesn&apos;t exist in our system. Let&apos;s get you back on track.
        </p>
        <div className="flex flex-wrap justify-center gap-4">
          <Button asChild variant="brand" className="h-12 px-8 font-bold">
            <Link href="/">Return to Mission Control</Link>
          </Button>
          <Button asChild variant="outline" className="h-12 px-8 font-bold">
            <Link href="/services">View Our Services</Link>
          </Button>
        </div>
      </div>
    </section>
  );
}
```

**error.tsx** — update button copy and add secondary "Return Home" link. Keep "use client" directive, GlobalError interface (`error` and `reset` props), badge, heading. Change:
- Reset button text from "Retry Protocol" to "Try Again"
- Add after the reset button:
```tsx
<Button asChild variant="outline" className="px-8 h-14 font-bold">
  <Link href="/">Return Home</Link>
</Button>
```
Keep all other code unchanged (badge, heading, subtitle, Navbar/Footer imports, error.digest display).
  </action>

  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/app/not-found.tsx exists
    - not-found.tsx does NOT contain "Navbar" or "Footer" as imports
    - not-found.tsx contains text "automation void"
    - not-found.tsx contains text "404 — Signal Lost"
    - not-found.tsx contains text "Return to Mission Control"
    - thank-you/page.tsx contains "Explore Services"
    - thank-you/page.tsx contains "Read Our Blog"
    - error.tsx contains "Try Again"
    - error.tsx contains "Return Home"
    - error.tsx still contains "reset()" (GlobalError interface preserved)
    - TypeScript compiles with no errors from these 3 files
  </acceptance_criteria>
  <done>Three static pages updated/created: 404 branded with quirky copy, thank-you has secondary nav, error page has correct button copy and Return Home link.</done>
</task>

<task type="auto">
  <name>Task 2: Expand Privacy Policy and Terms & Conditions with proper legal content</name>
  <files>frontend/src/app/privacy-policy/page.tsx, frontend/src/app/terms-and-conditions/page.tsx</files>

  <read_first>
    - frontend/src/app/privacy-policy/page.tsx (current implementation — expand content sections, keep page shell)
    - frontend/src/app/terms-and-conditions/page.tsx (current implementation — expand content sections, keep page shell)
  </read_first>

  <action>
Both pages keep the same page shell (Navbar, Footer, hero section with badge/heading/subtitle, radial gradient). Only the content section cards are replaced with expanded legal content.

**privacy-policy/page.tsx** — replace the `<section className="py-12">` block content cards with these expanded sections (keep container and `max-w-5xl`):

1. **Information We Collect** — Contact details, company name, project requirements, usage analytics from website interactions, IP address and browser data for security purposes.

2. **How We Use Your Information** — Deliver AI automation and digital marketing services; respond to inquiries; send relevant project proposals; improve platform performance; ensure legal compliance and fraud prevention; send service updates (opt-out available).

3. **AI Data Processing** — Digi Pexel uses AI tools including large language models and automation platforms (n8n, Make, Zapier) to process workflow data. Client data processed through these systems is handled with strict access controls. We do not use your data to train AI models without explicit consent.

4. **Third-Party Tools** — We use the following third-party services that may process your data: Google Analytics (website analytics), Calendly (booking and scheduling), email service providers (newsletter delivery). Each third party has their own privacy policy governing their data practices.

5. **GDPR and Data Rights** — If you are located in the EU/EEA: you have the right to access, rectify, erase, restrict, or port your data; you may withdraw consent at any time; you have the right to lodge a complaint with a supervisory authority. Contact us at info@digipexel.com to exercise these rights.

6. **Data Security** — We use TLS encryption for data in transit, access controls, and regular security reviews. Only authorized team members access client data for delivery or support purposes.

7. **Data Retention** — We retain client data for the duration of the service engagement and up to 3 years after project completion for legal and accounting purposes. Inquiry data is retained for up to 12 months.

8. **Contact** — For privacy requests: info@digipexel.com. Response within 5 business days.

**terms-and-conditions/page.tsx** — replace the content section cards with these expanded sections:

1. **Service Usage** — You agree to use Digi Pexel services in compliance with applicable laws. You are responsible for content, data, and configurations you provide. Prohibited: illegal activities, misrepresentation, reverse-engineering our proprietary systems.

2. **Deliverables and Timelines** — Project scope, milestones, and timelines are defined in written statements of work. Scope changes require a written change order. Delays caused by the client (late approvals, missing assets) adjust timelines accordingly and do not constitute breach by Digi Pexel.

3. **Intellectual Property** — Upon full payment, client owns all custom deliverables created specifically for their project. Digi Pexel retains ownership of pre-existing tools, frameworks, and reusable components embedded in deliverables (licensed for client use). Third-party tools and libraries remain under their respective licenses.

4. **Payments** — Fees and billing schedules are defined in your proposal or agreement. A deposit (typically 50%) is due before work commences. Remaining balance is due on delivery or per milestone schedule. Late payments exceeding 14 days may pause active delivery.

5. **Service Level and Liability** — Digi Pexel targets response times of 24–48 hours for support inquiries during active projects. We are not liable for: indirect or consequential damages; data loss caused by third-party platforms; outages of external tools (Google, Meta, n8n, etc.); results dependent on client-provided assets or access. Our maximum liability is limited to fees paid in the 30 days preceding the claim.

6. **Confidentiality** — Both parties agree to keep proprietary information, business strategies, and client data confidential during and for 2 years after the project. This does not apply to publicly available information.

7. **Termination** — Either party may terminate the engagement with 14 days written notice. Client is responsible for fees covering work completed through the termination date. Pre-paid fees for unstarted work are refunded within 14 days.

8. **Governing Law** — These terms are governed by applicable laws. Disputes are first subject to good-faith negotiation, then mediation. Contact: info@digipexel.com.

Use the same card style as the existing pages: `rounded-2xl border border-border-subtle bg-surface p-8`. Section headings use `text-xl font-semibold text-primary mb-3 mt-0` (not `.section-title` — too large for in-page section headers). Body text uses `text-secondary text-sm leading-relaxed`.
  </action>

  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - privacy-policy/page.tsx contains "GDPR"
    - privacy-policy/page.tsx contains "Calendly"
    - privacy-policy/page.tsx contains "AI Data Processing" or "AI tools"
    - privacy-policy/page.tsx contains "Google Analytics"
    - terms-and-conditions/page.tsx contains "Intellectual Property"
    - terms-and-conditions/page.tsx contains "liability"
    - terms-and-conditions/page.tsx contains "Termination"
    - terms-and-conditions/page.tsx contains "Confidentiality"
    - Both files retain the hero section with Badge, h1, and section-subtitle elements
    - TypeScript compiles with no errors from these 2 files
  </acceptance_criteria>
  <done>Privacy Policy and Terms & Conditions contain professional legal content appropriate for a digital marketing automation agency covering AI data processing, third-party tools, GDPR, IP ownership, SLAs, liability, and termination.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `frontend/src/app/not-found.tsx` exists and contains "automation void" text
2. `not-found.tsx` has NO Navbar or Footer imports
3. `thank-you/page.tsx` has "Explore Services" and "Read Our Blog" secondary buttons
4. `error.tsx` has "Try Again" and "Return Home" 
5. Privacy Policy has GDPR and Calendly sections
6. Terms has IP, liability, and termination sections
7. `cd frontend && npx tsc --noEmit` exits 0
</verification>

<success_criteria>
- All 5 static/legal files complete
- Not-found.tsx compiles as a Server Component with no Navbar/Footer import
- Legal pages contain expanded content appropriate for a digital marketing automation agency
- TypeScript strict mode passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/07-seo-static-pages-launch-readiness/07-01-SUMMARY.md`
</output>
