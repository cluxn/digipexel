---
phase: 07-seo-static-pages-launch-readiness
plan: 05
type: execute
wave: 3
depends_on: [07-01, 07-03, 07-04]
files_modified: []
autonomous: false
requirements: [SEO-06]

must_haves:
  truths:
    - "All 5 static pages render correctly in the browser with design system styling"
    - "Calendly popup opens when clicking a Book a Call CTA (requires calendly_url configured)"
    - "sitemap.xml is accessible at https://digipexel.cluxn.com/sitemap.xml after deploy"
    - "robots.txt disallows /admin at https://digipexel.cluxn.com/robots.txt"
    - "Page speed audit shows no critical issues blocking launch"
    - "JSON-LD Organization schema appears in page source on homepage"
  artifacts:
    - path: "frontend/out/sitemap.xml"
      provides: "Generated sitemap after npm run build"
    - path: "frontend/out/robots.txt"
      provides: "Generated robots.txt after npm run build"
    - path: "frontend/out/404.html"
      provides: "Custom 404 page compiled from not-found.tsx"
  key_links:
    - from: "npm run build"
      to: "frontend/out/sitemap.xml"
      via: "postbuild next-sitemap"
      pattern: "sitemap.xml"
    - from: "app/layout.tsx Organization schema"
      to: "view-source: homepage"
      via: "Server Component render — must appear in raw HTML"
      pattern: "application/ld\\+json"
---

<objective>
Verify all Phase 7 deliverables work correctly: visual review of static pages, build verification (sitemap/robots/404.html), Calendly popup test, schema in page source, and page speed audit.

Purpose: SEO-06 and final launch readiness gate — confirm everything built in Plans 01–04 is working before the site goes live.
Output: Human sign-off on all verifications; page speed audit documented.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-seo-static-pages-launch-readiness/07-CONTEXT.md
@.planning/phases/07-seo-static-pages-launch-readiness/07-01-SUMMARY.md
@.planning/phases/07-seo-static-pages-launch-readiness/07-02-SUMMARY.md
@.planning/phases/07-seo-static-pages-launch-readiness/07-03-SUMMARY.md
@.planning/phases/07-seo-static-pages-launch-readiness/07-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build verification — compile site and check output artifacts</name>
  <files></files>

  <read_first>
    - frontend/package.json (confirm postbuild script is wired)
    - frontend/next-sitemap.config.js (confirm outDir: ./out)
  </read_first>

  <action>
Run the full build and verify output artifacts:

```bash
cd frontend && npm run build
```

After build completes, verify:
1. `ls frontend/out/sitemap.xml` — file must exist
2. `ls frontend/out/robots.txt` — file must exist
3. `ls frontend/out/404.html` — file must exist (from not-found.tsx)
4. `grep "Disallow: /admin" frontend/out/robots.txt` — must match
5. `grep "application/ld+json" frontend/out/index.html` — Organization schema in homepage source
6. `grep "digipexel.cluxn.com" frontend/out/sitemap.xml` — site URLs in sitemap

If the build fails, diagnose and fix:
- TypeScript errors → fix in respective files
- Module not found errors → check imports
- Static generation errors → check generateMetadata fetch failures (should catch() gracefully)
- next-sitemap output to wrong dir → check outDir in next-sitemap.config.js

Report findings.
  </action>

  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - npm run build exits 0 (no error)
    - frontend/out/sitemap.xml exists after build
    - frontend/out/robots.txt exists after build
    - frontend/out/404.html exists after build
    - frontend/out/robots.txt contains "Disallow: /admin"
    - frontend/out/index.html contains "application/ld+json"
    - frontend/out/index.html contains "Organization"
  </acceptance_criteria>
  <done>Build succeeds, sitemap.xml and robots.txt generated in out/, 404.html exists, Organization schema in homepage source.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 7 complete deliverables:
    - 5 static pages redesigned (404, thank-you, error, privacy-policy, terms)
    - generateMetadata on all public pages (build-time SEO meta)
    - Organization + Service JSON-LD schemas
    - Calendly integration in connect-cta.tsx (with settings.php calendly_url key)
    - Admin /admin/seo page with per-page meta editor
    - Sitemap.xml + robots.txt via next-sitemap postbuild
  </what-built>

  <how-to-verify>
    Run local dev server:
    ```
    cd frontend && npm run dev
    ```

    **A. Static Pages (STATIC-01..05)**
    1. Visit http://localhost:3000/thank-you — confirm "Transmission Success" heading, "Explore Services" and "Read Our Blog" secondary buttons visible
    2. Visit http://localhost:3000/non-existent-route — confirm 404 page shows "Lost in the automation void?" heading with "404 — Signal Lost" badge and two CTA buttons
    3. Confirm 404 page has Navbar + Footer from root layout (not imported manually)
    4. Visit privacy-policy — confirm GDPR section and Calendly/Google Analytics mentions are present
    5. Visit terms-and-conditions — confirm IP, liability, and termination sections are present

    **B. Admin SEO Page (SEO-01)**
    6. Visit http://localhost:3000/admin/seo (log in if needed: code 12345)
    7. Confirm amber rebuild notice banner is visible at top
    8. Select "Blog Listing" from dropdown — fields should load (empty if no meta saved)
    9. Enter a test title, save — confirm "Meta saved. Rebuild to apply changes." success message

    **C. Calendly Integration (SEO-05)**
    10. In admin Settings, add your Calendly URL to the new "Calendly URL" field (the admin/settings page may not yet show this field — if the field is missing, go to admin/seo and also check Settings page)
    11. OR: directly in admin settings DB add calendly_url key
    12. Reload any page with a "Start Building Now" / "Book a Call" CTA
    13. Click the CTA — Calendly popup should open
    14. If no Calendly URL is configured yet, confirm the CTA still works as a regular link (fallback behavior)

    **D. Build artifacts (SEO-02/03)**
    15. After `npm run build`, confirm frontend/out/sitemap.xml exists
    16. Open frontend/out/sitemap.xml — confirm public page URLs are listed
    17. Open frontend/out/robots.txt — confirm "Disallow: /admin/" line is present
    18. Open frontend/out/404.html — confirm it contains "automation void" text

    **E. Page Speed Audit (SEO-06)**
    19. After the site is deployed to https://digipexel.cluxn.com, run Google PageSpeed Insights: https://pagespeed.web.dev/
    20. Enter the homepage URL — review scores
    21. Note any critical issues (CLS, LCP, FID scores) that would block launch
    22. Known expected issues: Framer Motion bundle size, unoptimized images (already set in next.config.ts), third-party Calendly script
    23. Report the Performance score — anything above 50 mobile / 70 desktop is acceptable for launch

    **F. Schema in page source**
    24. In the browser, right-click → View Page Source on the homepage
    25. Search for "application/ld+json" — should find the Organization schema in the raw HTML
    26. Optional: test with Google Rich Results Test at https://search.google.com/test/rich-results
  </how-to-verify>

  <resume-signal>
    Type "approved" if all checks pass, or describe any issues found (e.g., "404 page missing navbar", "Calendly popup not opening", "build failed with error: ...").
  </resume-signal>
</task>

</tasks>

<verification>
Phase 7 is verified when:
1. Build succeeds with sitemap.xml, robots.txt, 404.html in out/
2. All 5 static pages render correctly with design system components
3. Admin SEO page saves and shows rebuild notice
4. Calendly popup fires (or fallback link works when URL not set)
5. Organization JSON-LD appears in homepage page source
6. Page speed audit documented with no critical blocking issues
</verification>

<success_criteria>
- Human approves all visual and functional verifications
- Build artifacts confirmed in out/ directory
- Page speed audit documented (score noted, no critical issues that block launch)
- Phase 7 complete — site is launch-ready
</success_criteria>

<output>
After completion, create `.planning/phases/07-seo-static-pages-launch-readiness/07-05-SUMMARY.md`
</output>
