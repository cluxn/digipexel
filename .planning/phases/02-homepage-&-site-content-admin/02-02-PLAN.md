---
phase: 02-homepage-site-content-admin
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/blocks/services.tsx
  - frontend/src/components/blocks/context-problem.tsx
autonomous: true
requirements:
  - HOME-04
  - HOME-05
  - HOME-06
  - HOME-07

must_haves:
  truths:
    - "The services section eyebrow label reads 'What We Do' — not 'Our Ecosystem'"
    - "All 11 service cards in services.tsx have non-# hrefs pointing to their correct /services/* routes"
    - "The problem section subtitle fits within 2 lines at normal viewport width"
    - "The logo marquee shows at least 5 tech-relevant partner logos when the homepage loads"
  artifacts:
    - path: "frontend/src/components/blocks/services.tsx"
      provides: "Services section with updated eyebrow label and verified hrefs"
      contains: "What We Do"
    - path: "frontend/src/components/blocks/context-problem.tsx"
      provides: "Problem section with 2-line subtitle"
  key_links:
    - from: "services.tsx service cards"
      to: "/services/* routes"
      via: "Link href prop on each ServiceCard"
      pattern: "href.*services/"
---

<objective>
Fix four quick content issues on the homepage that require only code edits — no API, no new files.

Purpose: These are copy/link correctness fixes. They are independent of the backend built in Plan 01 and can ship in parallel.
Output: Updated eyebrow label in services.tsx, verified service hrefs, 2-line problem subtitle, partner logos added via admin logos UI
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-homepage-&-site-content-admin/02-CONTEXT.md

Key decisions:
- D-17: Logo marquee update is a DATA task via /admin/logos UI — add 8 logos: OpenAI, Anthropic, n8n, Zapier, Make.com, Google Ads, Meta, Microsoft
- D-18: Logo task requires no code changes — use admin UI or SQL
- D-19: services.tsx eyebrow "Our Ecosystem" → "What We Do". Review all other eyebrows for jargon.
- D-20: Verify services.tsx hrefs — all should be /services/* routes, none should be "#"
- D-21: context-problem.tsx subtitle: trim to max 2 lines. Current text: "Every manual handoff costs time, context, and revenue. We bridge every gap with AI that connects your entire stack and acts 24/7." This is already close — verify and tighten if needed.

Deferred (out of scope for this plan): Admin-editable eyebrows, admin-editable service cards
</context>

<interfaces>
<!-- Current state of files being modified. Read these before touching anything. -->

From frontend/src/components/blocks/services.tsx (current eyebrow, line ~63):
```tsx
<Badge variant="outline" className="section-eyebrow mb-4 px-6">
  Our Ecosystem   {/* ← change this to "What We Do" */}
</Badge>
```

All 11 service hrefs currently in services.tsx (verified non-# from research):
- strategic: "/services/custom-ai-solutions", "/services/ai-seo"
- social: "/services/youtube-automation", "/services/instagram-automation", "/services/linkedin-automation"
- workflow: "/services/automation-flows", "/services/ai-workflows", "/services/workflow-creation"
- operations: "/services/accounting-bookkeeping", "/services/hiring-recruitment", "/services/sales-automation"

From frontend/src/components/blocks/context-problem.tsx (current subtitle, line ~63):
```tsx
<p className="section-subtitle max-w-xl opacity-60">
  Every manual handoff costs time, context, and revenue. We bridge every gap with AI that connects your entire stack and acts 24/7.
</p>
```

Logo SVG URLs for admin data task:
| Brand    | SVG URL or display_type |
|----------|------------------------|
| OpenAI   | display_type: "text", name: "OpenAI" |
| Anthropic | display_type: "text", name: "Anthropic" |
| n8n      | display_type: "text", name: "n8n" |
| Zapier   | https://upload.wikimedia.org/wikipedia/commons/f/fd/Zapier_logo.svg |
| Google Ads | https://upload.wikimedia.org/wikipedia/commons/c/c7/Google_Ads_logo.svg |
| Meta     | https://upload.wikimedia.org/wikipedia/commons/7/7b/Meta_Platforms_Inc._logo.svg |
| Microsoft | display_type: "text", name: "Microsoft" |
| Make     | display_type: "text", name: "Make" |
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update services.tsx eyebrow label and verify all service hrefs</name>
  <files>frontend/src/components/blocks/services.tsx</files>
  <read_first>
    - frontend/src/components/blocks/services.tsx (read the full file to see current eyebrow text and all service href values before editing)
  </read_first>
  <action>
Two changes to services.tsx:

1. Change the eyebrow label from "Our Ecosystem" to "What We Do":
   Find: `Our Ecosystem`
   Replace: `What We Do`

2. Verify all service hrefs. Read the allServices object and confirm all 11 entries have non-# href values. The expected hrefs are:
   - /services/custom-ai-solutions
   - /services/ai-seo
   - /services/youtube-automation
   - /services/instagram-automation
   - /services/linkedin-automation
   - /services/automation-flows
   - /services/ai-workflows
   - /services/workflow-creation
   - /services/accounting-bookkeeping
   - /services/hiring-recruitment
   - /services/sales-automation

   If any href is "#" or missing, replace it with the correct /services/* route from the list above. Research confirms all hrefs are already correct — this is a verification step.

No other changes to services.tsx. Do not change copy, colors, icons, or layout.
  </action>
  <verify>
    <automated>cd "C:\Users\ASUS\Desktop\Digi Pexel\frontend" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/blocks/services.tsx contains the string "What We Do"
    - frontend/src/components/blocks/services.tsx does NOT contain the string "Our Ecosystem"
    - frontend/src/components/blocks/services.tsx contains exactly 11 href values starting with "/services/"
    - frontend/src/components/blocks/services.tsx does NOT contain href="#" in any ServiceCard
    - npx tsc --noEmit exits 0 (no TypeScript errors)
  </acceptance_criteria>
  <done>Services eyebrow reads "What We Do"; all 11 service card hrefs point to valid /services/* routes</done>
</task>

<task type="auto">
  <name>Task 2: Trim problem section subtitle to 2 lines</name>
  <files>frontend/src/components/blocks/context-problem.tsx</files>
  <read_first>
    - frontend/src/components/blocks/context-problem.tsx (read the full file to see current subtitle text and confirm the class containing it)
  </read_first>
  <action>
The current subtitle in context-problem.tsx is:
"Every manual handoff costs time, context, and revenue. We bridge every gap with AI that connects your entire stack and acts 24/7."

This is two sentences and roughly 2 lines at desktop viewport. Trim it to be tighter — maximum 1 line at desktop, 2 lines on mobile. Replace with:

"Every manual handoff costs time and revenue. We bridge every gap with AI that acts 24/7."

This cuts from ~22 words to ~16 words, keeping both the problem statement and the solution promise while fitting cleanly within 2 lines at all viewports. The container already has max-w-xl which limits width.

Find the paragraph containing "Every manual handoff" inside the ContextProblem section header block and replace only the text content. Do not change any className, surrounding elements, or other content in the file.

Do not touch any other text in context-problem.tsx — the PAIN_POINTS array, OUTCOMES array, stat cards, and section title are all out of scope.
  </action>
  <verify>
    <automated>cd "C:\Users\ASUS\Desktop\Digi Pexel\frontend" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/blocks/context-problem.tsx contains the string "Every manual handoff costs time and revenue"
    - frontend/src/components/blocks/context-problem.tsx does NOT contain "We bridge every gap with AI that connects your entire stack"
    - The updated subtitle paragraph still has className="section-subtitle max-w-xl opacity-60"
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Problem section subtitle is tightened to ~16 words that fit within 2 lines at normal viewport</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Add partner logos via admin logos UI</name>
  <what-built>
Tasks 1 and 2 automated all code changes. This task requires using the admin panel to update the logo marquee data — it cannot be scripted because the admin UI is auth-gated in a browser session.

The logo marquee already fetches from /api/logos.php (no code change needed per D-17). The data just needs to be updated to reflect real partner logos.
  </what-built>
  <how-to-verify>
1. Visit https://digipexel.cluxn.com/admin/logos (log in if prompted, passcode: 12345)
2. Remove all existing logos (Dish, Deloitte, Pfizer, Adobe, American Airlines, NBCUniversal) — click Trash icon for each
3. Add 8 new logos in this order:
   a. Name: "OpenAI", Display Mode: text, SVG URL: (leave blank)
   b. Name: "Anthropic", Display Mode: text, SVG URL: (leave blank)
   c. Name: "n8n", Display Mode: text, SVG URL: (leave blank)
   d. Name: "Zapier", Display Mode: image, SVG URL: https://upload.wikimedia.org/wikipedia/commons/f/fd/Zapier_logo.svg
   e. Name: "Google Ads", Display Mode: image, SVG URL: https://upload.wikimedia.org/wikipedia/commons/c/c7/Google_Ads_logo.svg
   f. Name: "Meta", Display Mode: image, SVG URL: https://upload.wikimedia.org/wikipedia/commons/7/7b/Meta_Platforms_Inc._logo.svg
   g. Name: "Microsoft", Display Mode: text, SVG URL: (leave blank)
   h. Name: "Make", Display Mode: text, SVG URL: (leave blank)
4. Click "Update Section" to save
5. Visit https://digipexel.cluxn.com (homepage) and confirm the marquee shows updated logos

Expected: Logo marquee scrolls showing "OpenAI", "Anthropic", "n8n", Zapier logo, Google Ads logo, Meta logo, "Microsoft", "Make"

Note: If init_db.php has not yet been re-visited since Plan 01 deployment, visit https://digipexel.cluxn.com/backend/init_db.php first to create the new tables.
  </how-to-verify>
  <resume-signal>Type "logos updated" when the marquee shows the new partner logos, or describe any issue</resume-signal>
</task>

</tasks>

<verification>
After tasks 1 and 2 complete (automated) and task 3 completes (manual):
1. grep "What We Do" frontend/src/components/blocks/services.tsx — finds 1 match
2. grep "Our Ecosystem" frontend/src/components/blocks/services.tsx — finds 0 matches
3. grep 'href="/services/' frontend/src/components/blocks/services.tsx | wc -l — outputs 11
4. grep "Every manual handoff costs time and revenue" frontend/src/components/blocks/context-problem.tsx — finds 1 match
5. Visual: homepage marquee shows updated partner logos
</verification>

<success_criteria>
- services.tsx eyebrow reads "What We Do" (HOME-05, HOME-06)
- All 11 service hrefs are /services/* routes — none are "#" (HOME-06)
- context-problem.tsx subtitle is tightened to ≤ 16 words (HOME-07)
- Logo marquee shows 8 updated tech partner logos on the live site (HOME-04)
- npx tsc --noEmit passes for both modified files
</success_criteria>

<output>
After completion, create `.planning/phases/02-homepage-site-content-admin/02-02-SUMMARY.md`
</output>
